const MAX_IMAGE_SIZE_MB = 5;
export const MAX_IMAGE_SIZE_BYTES = MAX_IMAGE_SIZE_MB * 1024 * 1024;
export const MAX_GALLERY_BULK_UPLOAD = 20;
export const MAX_NEWS_IMAGES = 20;
export const MAX_IMAGE_SIZE_LABEL = `${MAX_IMAGE_SIZE_MB}MB`;
export const IMAGE_SIZE_ERROR = `Image size must be less than ${MAX_IMAGE_SIZE_MB}MB`;
export const LOGO_SIZE_ERROR = `Logo size must be less than ${MAX_IMAGE_SIZE_MB}MB`;
export const SECTION_IMAGE_SIZE_ERROR = `Section image must be less than ${MAX_IMAGE_SIZE_MB}MB`;
